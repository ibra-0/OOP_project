/**
 * Application services: persistence, authentication, course enrollment, marks, and messaging.
 * Most services are singletons accessed via {@link services.ServiceRegistry}.
 */
package services;
